# Supabase Configuration
# Copy this file to .env and fill in your credentials

# Supabase Project URL
# Get from: Supabase Dashboard → Settings → API → Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Service Role Key (for export_to_supabase.py)
# Get from: Supabase Dashboard → Settings → API → Service Role Key
# ⚠️ KEEP THIS SECRET! Never commit to Git or expose client-side!
SUPABASE_KEY=eyJhbGc...your-service-role-key

# Optional: Custom API endpoint (if using Edge Functions)
SUPABASE_API_ENDPOINT=

# Notes:
# - For client-side (js/config.js), use Anon/Public Key (safe)
# - For server-side (export_to_supabase.py), use Service Role Key (dangerous if exposed)
# - Free tier includes: 500 MB database, 1 GB file storage, 2 GB bandwidth
